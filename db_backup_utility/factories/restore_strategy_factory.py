from db_backup_utility.factories.backup_strategy_factory import BackupStrategyFactory


class RestoreStrategyFactory(BackupStrategyFactory):
    """Restore strategies share the database strategy implementations."""

